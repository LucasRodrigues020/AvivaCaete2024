// No seu arquivo contador.js
let contador = 0;

function incrementarContador() {
    contador++;
    // Adicione aqui o código para enviar o contador para algum lugar (pode ser um servidor, armazenamento na nuvem, etc.)
}
